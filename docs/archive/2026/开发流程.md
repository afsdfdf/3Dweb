1. Codex 执行批次建议

建议按这个顺序喂：

Batch 1：架构收口
统一存储配置来源
统一安全配置来源
收口 Canonical URL 的生产行为
Batch 2：数据库与商务域整洁
支付域命名兼容层继续推进
积分账本 PostgreSQL 专项兼容
模型资产优先落自有存储
Batch 3：后台管理优化
后台角色与危险操作隔离
运营动作后台增强
审计日志
Batch 4：稳定性护栏
高成本接口限流
单元测试扩展
集成测试扩展
2. 后端专项整改任务清单（直接给 Codex）
任务 1：统一存储配置来源，移除运行时双轨
请在当前仓库中做“存储配置来源统一”整改。

背景：
当前项目已经存在独立的 `StorageSettings` global，但运行时仍有双轨：
- `src/globals/StorageSettings.ts` 已经存在
- `src/lib/databaseRuntimeConfig.ts` 中有 `readS3StorageSettings()`
- 另有 `src/lib/s3Settings.ts` 仍在读取另一套配置逻辑

目标：
1. 统一对象存储运行时读取来源
2. 明确职责：
   - env：真实 secrets（AWS access key / secret key）
   - StorageSettings：非敏感配置（bucket / region / prefix / baseURL / signedDownloads）
3. 避免出现两套存储配置同时生效
4. 明确哪些地方在构建时读取，哪些地方在运行时读取
5. 不破坏现有 S3 plugin 和下载链路

范围：
- `src/globals/StorageSettings.ts`
- `src/lib/s3Settings.ts`
- `src/lib/databaseRuntimeConfig.ts`
- `src/payload.config.ts`
- 如有必要补文档说明

验收要求：
1. 只有一套非敏感存储配置来源
2. 真实 access key / secret key 继续只来自 env
3. 下载逻辑与 S3 plugin 不退化
4. 配置缺失时报错明确
5. 增加测试或最小验证脚本

输出要求：
- 先说明当前双轨问题
- 再给统一后的配置架构
- 最后改代码和补测试
任务 2：统一安全配置来源，让 SecuritySettings 真正生效
请在当前仓库中做“安全配置中心接线”整改。

背景：
当前项目已经存在 `src/globals/SecuritySettings.ts`，但运行中的请求安全逻辑仍主要依赖环境变量或本地工具。
需要让后台的 SecuritySettings 成为真正的非敏感安全配置来源。

目标：
1. 让以下配置优先从 `SecuritySettings` 读取：
   - allowedMutationOrigins
   - allowedRemoteAssetHosts
2. 环境变量仍可作为 fallback 或引导迁移来源，但不应长期作为唯一来源
3. 不破坏现有：
   - `src/lib/requestSecurity.ts`
   - `src/lib/remoteAssetSecurity.ts`
4. 明确读取优先级：
   - Global 配置
   - env fallback
   - 默认值
5. 保持生产行为安全

范围：
- `src/globals/SecuritySettings.ts`
- `src/lib/requestSecurity.ts`
- `src/lib/remoteAssetSecurity.ts`
- 如有必要新增 helper

验收要求：
1. 后台保存的 allowed origins 能影响运行时
2. 后台保存的 allowed remote hosts 能影响下载安全策略
3. 环境变量仍可兼容旧部署
4. 增加单元测试

输出要求：
- 先分析当前配置未真正接线的问题
- 再设计读取优先级
- 再改代码和补测试
任务 3：收紧 getCanonicalAppURL() 的生产行为
请在当前仓库中重构 `src/lib/getCanonicalAppURL.ts`，只针对后端行为，不碰前端。

背景：
当前 `getCanonicalAppURL()` 已存在，但在配置非法或缺失时仍可能回退到本地默认值。
这对生产环境中的 Stripe / AI provider / 回跳地址不够严格。

目标：
1. 开发环境允许 localhost fallback
2. 生产环境中如果 canonical URL 缺失或非法，必须抛出明确错误
3. 保持以下调用点可用：
   - `src/lib/printOrderFlow.ts`
   - `src/lib/stripeBilling.ts`
   - `src/lib/meshyGateway.ts`
4. 不再在生产静默回退到 `127.0.0.1`

范围：
- `src/lib/getCanonicalAppURL.ts`
- 相关测试文件
- 如有必要更新 `.env.example`

验收要求：
1. 开发环境 fallback 正常
2. 生产环境配置错误会明确报错
3. 现有业务 URL 构造不退化
4. 测试覆盖开发/生产两种行为

输出要求：
- 先分析风险
- 再改代码
- 再补测试
任务 4：推进支付域中性化，减少 Shopify 历史字段直接使用
请在当前仓库中继续推进支付域中性化，但不要做破坏性迁移。

背景：
当前仓库主支付链路实际以 Stripe 为主，但底层仍保留：
- `ShopifyPayments`
- `shopifyOrderId`
- `shopifyCheckoutUrl`
- `shopifyCustomerId`
这些 legacy 名称会降低数据库与业务层整洁度。

目标：
1. 保留当前 collection 与旧字段兼容
2. 增强或新增中性 helper，统一这些业务概念：
   - provider
   - sessionId
   - checkoutUrl
   - orderReference
   - customerReference
3. 优先减少这些位置对 legacy 字段的直接依赖：
   - `src/lib/printOrderFlow.ts`
   - `src/endpoints/stripeWebhook.ts`
   - `src/lib/subscriptionFlow.ts`
   - `src/lib/paymentRecords.ts`
4. 不改变现有数据库结构，不做重命名 migration
5. 让后台和代码里尽量使用中性语义

范围：
- `src/lib/paymentRecords.ts`
- `src/lib/printOrderFlow.ts`
- `src/endpoints/stripeWebhook.ts`
- `src/lib/subscriptionFlow.ts`
- 如有必要少量调整 collection admin 文案

验收要求：
1. 核心业务代码尽量不再直接使用 legacy 字段名
2. 支付、订阅、订单链路不退化
3. 兼容历史数据
4. 增加最小测试

输出要求：
- 先说明兼容策略
- 再替换调用点
- 不要急着改 slug
任务 5：对积分账本做 PostgreSQL 专项兼容与稳定性改造
请在当前仓库中对积分账本做 PostgreSQL 专项兼容与稳定性改造。

背景：
当前项目同时支持 PostgreSQL / SQLite。
积分账本设计已经比较完整，有 Credits 账户和 CreditTransactions 流水，但需要更严格验证与收口 PostgreSQL 下的行为。

目标：
1. 审查 `src/lib/creditLedger.ts` 的事务、锁、原子更新逻辑
2. 明确哪些逻辑是 SQLite 友好的，哪些在 PostgreSQL 下需要专门处理
3. 保持现有公开 API 不变
4. 保证：
   - 幂等
   - 原子更新
   - reservedBalance 一致
   - users.creditsBalance 镜像一致
5. 如有必要，抽出 DB 适配 helper

范围：
- `src/lib/creditLedger.ts`
- 如有必要新增 `src/lib/ledgerStore.ts`
- 如有必要补文档

验收要求：
1. grant / reserve / settle / refund 不退化
2. 重复 idempotencyKey 不会重复记账
3. PostgreSQL 场景下可运行
4. SQLite 场景不退化
5. 补单元测试或集成测试

输出要求：
- 先说明现有实现的风险点
- 再设计兼容方案
- 再改代码和补测试
任务 6：让 AI 成功结果优先落到自有存储
请在当前仓库中为 AI 成功结果增加“优先落入自有存储”的能力。

背景：
当前模型下载已经有远程资源 allowlist，但长期不应依赖第三方 provider URL 作为最终交付源。
更稳妥的做法是：生成成功后就把结果拉入自己的 Media / S3，再从本地资源交付。

目标：
1. 在 AI 任务成功后，增加结果入库步骤
2. 拉取 provider 返回的模型文件
3. 保存到自有 Media / S3
4. 更新 `Models.formats.file` 指向自有资源
5. 若存储不可用，允许明确 fallback，但不能静默失败

范围：
- `src/lib/aiTaskFlow.ts`
- `src/collections/Models.ts`
- `src/collections/Media.ts`（如有必要）
- 相关存储 helper

验收要求：
1. 至少一种格式文件可落自有存储
2. 下载优先走本地资源
3. 存储未配置时行为明确
4. 不破坏现有任务成功逻辑

输出要求：
- 先说明资产入库策略
- 再说明 fallback 策略
- 再实现
任务 7：细化后台角色权限，区分 admin 与 operator 危险能力
请在当前仓库中细化后台角色权限，重点区分 admin 与 operator 的危险操作边界。

背景：
当前项目已有 admin / operator / customer 三角色，后台 admin access 已经允许 admin 和 operator 进入。
但很多 `isStaff` 逻辑仍把 admin/operator 统一处理，不利于危险操作隔离。

目标：
1. 梳理所有当前 `isStaff` 保护的高危操作
2. 明确 admin 专属能力，例如：
   - 修改系统级配置
   - 调整高敏账务
   - 修改用户角色
   - 修改安全配置
3. operator 只保留运营动作，例如：
   - 查看任务
   - 查看订单
   - 管理内容
   - 处理非高危后台动作
4. 不破坏当前普通用户权限

范围：
- `src/access/index.ts`
- `src/collections/Users.ts`
- `src/globals/AIProviderSettings.ts`
- `src/globals/StorageSettings.ts`
- `src/globals/SecuritySettings.ts`
- 其他高危写入位置

验收要求：
1. admin/operator/customer 边界更清晰
2. 高危配置只允许 admin
3. operator 保留必要运营能力
4. 增加权限测试或最小验证

输出要求：
- 先列出当前 admin/operator 混用点
- 再设计角色矩阵
- 再改代码
任务 8：把后台从“技术后台”增强为“动作型运营后台”
请在当前仓库中增强后台运营能力，不改前端公开页面。

背景：
当前项目已有 Payload Admin 和 `ops dashboard`，但仍偏统计和 CRUD，不足以支撑日常运营动作。

目标：
1. 增强后台运营动作能力，优先支持：
   - 失败任务筛选
   - 失败任务人工重试入口（如果当前技术条件允许）
   - 支付异常记录查看
   - 订单异常状态查看
   - 高余额/低余额用户快速筛选
2. 若不能直接做完整动作，也要先把后台视图和筛选能力整理好
3. 不做前台页面

范围：
- `src/lib/adminDashboard.ts`
- `src/endpoints/opsDashboard.ts`
- 如有必要新增 admin helper
- 如有必要少量调整 collection admin 配置

验收要求：
1. 后台能更方便定位异常任务/订单/支付
2. 运营人员可以更快筛查问题
3. 不影响现有 admin dashboard

输出要求：
- 先说明当前后台“偏技术后台”的问题
- 再设计运营增强点
- 再改代码
任务 9：新增统一审计日志
请在当前仓库中新增统一审计日志能力，重点覆盖后端高价值流程。

背景：
当前后端已有支付、积分账本、Webhook、订单、AI 任务、内部 overrideAccess 操作，但审计信息仍比较分散。

目标：
1. 新增统一审计日志工具，例如 `src/lib/auditLog.ts`
2. 至少覆盖这些事件：
   - AI webhook 验签成功/失败
   - Stripe webhook 处理结果
   - 积分预扣 / 结算 / 退款
   - 打印订单状态变化
   - 订阅发积分
3. 日志字段至少包含：
   - eventType
   - userId（如有）
   - taskId/orderId/sessionId（如有）
   - status
   - timestamp
4. 先统一 logger 接口，不强制先落数据库

范围：
- 新增 `src/lib/auditLog.ts`
- 修改相关 flow / endpoint 文件

验收要求：
1. 关键后端链路都有结构化日志
2. 出错时能追溯 user/task/order/session
3. 不影响现有主流程

输出要求：
- 先定义审计事件模型
- 再接入关键点
任务 10：为高成本接口增加基础限流
请在当前仓库中为高成本接口增加基础限流能力。

背景：
当前后端已经有 AI 任务、订单、订阅这些高成本接口，但仓库里还没有正式的限流实现。

目标：
1. 新增基础限流工具，例如 `src/lib/rateLimit.ts`
2. 至少为以下接口增加限流：
   - `/studio/ai/tasks`
   - `/studio/ai/tasks/:taskId/sync`
   - `/billing/subscriptions/checkout`
   - `/commerce/print-orders`
3. key 至少支持：
   - 登录用户 id
   - 未登录时 IP
4. 超限返回明确状态码和消息
5. 实现可以先用内存，但要预留 Redis 替换接口

范围：
- 对应 endpoint 文件
- 新增 `src/lib/rateLimit.ts`

验收要求：
1. 正常请求可通过
2. 超限请求被拒绝
3. 不同用户不会互相污染
4. 后续可替换成 Redis

输出要求：
- 先说明当前限流策略
- 再改代码
- 再补最小测试
任务 11：补足后端单元测试
请在当前仓库中扩展后端单元测试，只做后端，不做前端。

背景：
当前项目已经有 `test:unit`，也已有 `requestSecurity` 和 `getCanonicalAppURL` 的测试基础。
但后端核心域还缺少足够的护栏。

目标：
1. 继续沿用 node:test 体系
2. 至少新增这些单元测试：
   - `webhookSignature`
   - `remoteAssetSecurity`
   - `getCanonicalAppURL` 的生产环境失败行为
   - payment helper 的纯函数/转换逻辑
   - 账务层可隔离测试的逻辑
3. 目录结构清晰
4. 不引入重型测试框架，除非必要

范围：
- `tests/`
- 如有必要新增小型 test helper

验收要求：
1. `test:unit` 能跑
2. 新测试有明确覆盖目标
3. 重点覆盖后端安全与稳定性逻辑

输出要求：
- 先列出现有覆盖范围
- 再说明新增覆盖范围
- 再补测试
任务 12：新增后端集成测试，覆盖支付/账务/下载退款
请在当前仓库中新增第一批后端集成测试，不做前端 E2E。

背景：
当前仓库已经有 smoke test 和少量单元测试，但还缺少能真正托底核心后端主链路的集成测试。

目标：
1. 选择最轻量的集成测试方案
2. 第一批至少覆盖：
   - Stripe webhook 完成打印订单支付
   - 订阅发积分幂等
   - 模型下载失败自动退款
   - AI webhook 验签 + 任务更新
3. 优先 mock 外部依赖
4. 给出清晰运行方式

范围：
- 测试基础设施
- mock / fixture / helper
- 后端核心链路对应文件

验收要求：
1. 关键支付/账务/下载链路有自动化保护
2. 外部依赖可 mock
3. 运行命令清晰
4. 不要求一次覆盖前端页面

输出要求：
- 先说明测试方案
- 再列出覆盖场景
- 再实现
3. 给你的实际执行顺序

最值得先做的是这 6 个：

任务 1：统一存储配置来源
任务 2：统一安全配置来源
任务 4：推进支付域中性化
任务 5：积分账本 PostgreSQL 专项改造
任务 9：统一审计日志
任务 10：高成本接口限流

然后再做：

任务 6：资产落自有存储
任务 7：后台角色权限细化
任务 8：后台动作型运营增强
任务 11：单元测试扩展
任务 12：集成测试扩展
任务 3：Canonical URL 生产行为收紧（这个也可以提前）
4. 一句总结给 Codex

你现在后端最该做的，不是“再堆更多模块”，而是：

统一配置来源、收口支付域、打稳账务与存储、把后台变成真正可运营的后台。

---

## 修改记录

### 2026-04-17 任务 1：统一存储配置来源

- 变更范围：
  - `src/lib/s3Settings.ts`
  - `src/lib/databaseRuntimeConfig.ts`
  - `src/payload.config.ts`
  - `src/hooks/syncMediaToS3.ts`
  - `src/lib/s3SignedURL.ts`
  - `src/globals/StorageSettings.ts`
  - `.env.example`
  - `tests/s3Settings.test.ts`
- 调整内容：
  - 运行时非敏感存储配置统一只从 `StorageSettings` global 读取。
  - AWS `accessKeyId` / `secretAccessKey` 继续只从环境变量读取。
  - 移除运行时对 legacy `ai-provider-settings.storage` 和 `S3_*` 非敏感环境变量的依赖。
  - 启动期 `@payloadcms/storage-s3` 插件 bootstrap 单独通过 `readS3PluginBootstrapSettingsFromEnv()` 读取 env，并对半截配置抛出明确错误。
  - 媒体同步与签名下载在 `StorageSettings.enabled=true` 但配置不完整时输出明确错误日志，不再静默吞掉原因。
- 默认值与来源：
  - `bucket`：来源 `StorageSettings.bucket`，默认空字符串。
  - `region`：来源 `StorageSettings.region`，默认 `us-east-1`。
  - `prefix`：来源 `StorageSettings.prefix`，默认 `media`。
  - `baseURL`：来源 `StorageSettings.baseURL`，默认空字符串。
  - `signedDownloads`：来源 `StorageSettings.signedDownloads`，默认 `true`。
  - `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`：来源环境变量，无默认值。
- 迁移方式：
  - 先把现网使用的 bucket / region / prefix / baseURL / signedDownloads 写入 `Admin -> Globals -> Storage Settings`。
  - 校验运行时下载与媒体同步无误后，再逐步下线 `S3_BUCKET`、`S3_REGION`、`S3_PREFIX`、`S3_CDN_URL`、`S3_SIGNED_DOWNLOADS` 这些非敏感 bootstrap env。
  - 保留 `AWS_ACCESS_KEY_ID` 与 `AWS_SECRET_ACCESS_KEY` 作为长期 secrets 来源。

### 2026-04-17 任务 2：统一安全配置来源

- 变更范围：
  - `src/lib/securitySettings.ts`
  - `src/lib/requestSecurity.ts`
  - `src/lib/remoteAssetSecurity.ts`
  - `src/globals/SecuritySettings.ts`
  - `tests/requestSecurity.test.ts`
  - `tests/remoteAssetSecurity.test.ts`
- 调整内容：
  - 抽出统一安全配置 helper，集中读取 `SecuritySettings` global。
  - `allowedMutationOrigins` 读取优先级改为：`SecuritySettings.allowedMutationOrigins` > `ALLOWED_REQUEST_ORIGINS` > `CANONICAL_APP_URL/NEXT_PUBLIC_APP_URL`。
  - `allowedRemoteAssetHosts` 读取优先级改为：系统派生 host（站点域名、自有存储 host）+ `SecuritySettings.allowedRemoteAssetHosts`；当 global 为空时再回退 `AI_REMOTE_ASSET_ALLOWLIST`。
  - 保持旧环境变量可兼容旧部署，但后台配置一旦存在即成为运行时主来源。
- 默认值与来源：
  - `allowedMutationOrigins`：默认空数组；空时回退旧 env，再回退 canonical/public URL。
  - `allowedRemoteAssetHosts`：默认空数组；空时回退旧 env，并始终包含 canonical host / S3 bucket host / storage baseURL host。
- 迁移方式：
  - 将现网 `ALLOWED_REQUEST_ORIGINS` 和 `AI_REMOTE_ASSET_ALLOWLIST` 搬入 `Admin -> Globals -> Security Settings`。
  - 验证后台保存后运行时请求安全与远程资源 allowlist 行为正确，再逐步下线旧 env allowlist。

### 2026-04-17 任务 4：推进支付域中性化

- 变更范围：
  - `src/lib/paymentRecords.ts`
  - `src/lib/printOrderFlow.ts`
  - `tests/paymentRecords.test.ts`
- 调整内容：
  - 扩展支付兼容层 helper，统一中性语义：`provider`、`sessionId`、`checkoutUrl`、`orderReference`、`customerReference`。
  - 新增 `setPaymentLegacyFields()`，由 helper 负责把中性字段回写到历史 `shopify*` 字段。
  - `printOrderFlow` 改为通过 helper 读写 checkout/session 相关字段，减少业务代码直接出现 `shopifyCheckoutId/shopifyCheckoutUrl`。
- 兼容策略：
  - 不改 collection slug，不改数据库字段名，不做 migration。
  - 所有历史数据继续通过兼容 helper 读取。

### 2026-04-17 任务 5：积分账本 PostgreSQL 兼容与稳定性改造

- 变更范围：
  - `src/lib/ledgerStore.ts`
  - `src/lib/creditLedger.ts`
  - `tests/ledgerStore.test.ts`
  - `tests/creditLedger.test.ts`
- 调整内容：
  - `LedgerExecutor` 增加方言标识，区分 SQLite / PostgreSQL。
  - PostgreSQL 读账户时使用 `FOR UPDATE`，建账户时使用 `ON CONFLICT (user_id) DO NOTHING`，缩小并发窗口。
  - 幂等流水改为先插入 `credit_transactions` 再更新余额；若撞唯一约束则稳定返回 `applied: false`，避免先改余额再冲突。
  - `settleReservedTaskCredits` / `refundTaskCredits` 改为显式校验 `reservedBalance`，不再静默 `Math.max(0, ...)` 吞掉账务异常。
- 风险说明：
  - 这轮主要收口并发语义与不变量，未改公开 API。
  - 仍建议后续补更高强度的并发集成测试。

### 2026-04-17 任务 9：新增统一审计日志

- 变更范围：
  - `src/lib/auditLog.ts`
  - `src/endpoints/aiTasks.ts`
  - `src/endpoints/stripeWebhook.ts`
  - `src/lib/creditLedger.ts`
  - `src/lib/printOrderFlow.ts`
  - `src/lib/subscriptionFlow.ts`
- 调整内容：
  - 新增统一结构化审计 logger 接口 `writeAuditLog()`，当前先落 Payload logger，不落数据库。
  - 覆盖 AI webhook 验签/处理、Stripe webhook 处理结果、积分预扣/结算/退款/订阅发积分、打印订单支付成功后的状态变化。
  - 统一字段包括 `eventType/status/timestamp`，并按场景补充 `userId/taskId/orderId/sessionId/subscriptionId/provider/details`。

### 2026-04-17 任务 10：高成本接口基础限流复核

- 复核范围：
  - `src/lib/rateLimit.ts`
  - `src/lib/endpointRateLimit.ts`
  - `src/endpoints/aiTasks.ts`
  - `src/endpoints/subscriptions.ts`
  - `src/endpoints/printOrders.ts`
  - `tests/rateLimit.test.ts`
  - `tests/endpointRateLimit.test.ts`
- 复核结论：
  - `/studio/ai/tasks`
  - `/studio/ai/tasks/:taskId/sync`
  - `/billing/subscriptions/checkout`
  - `/commerce/print-orders`
  已存在正式限流实现，并且满足 userId / IP 键隔离、429 响应、后续可切 Redis 的要求。
  - 本轮未新增限流代码，保留现有实现，仅将其纳入后续任务验收范围。

### 2026-04-17 任务 3：收紧 Canonical URL 生产行为

- 变更范围：
  - `src/lib/getCanonicalAppURL.ts`
  - `tests/getCanonicalAppURL.test.ts`
  - `.env.example`
- 调整内容：
  - 开发环境保留 `http://127.0.0.1:3000` fallback。
  - 生产环境下如果 `CANONICAL_APP_URL` / `NEXT_PUBLIC_APP_URL` 缺失或非法，直接抛出明确错误，不再静默回退本地地址。
  - 保持 `printOrderFlow`、`stripeBilling`、`meshyGateway` 等现有调用点不变，仅收紧 helper 行为。
- 默认值与来源：
  - 开发环境默认值：`http://127.0.0.1:3000`
  - 生产环境：必须提供 `CANONICAL_APP_URL` 或合法的 `NEXT_PUBLIC_APP_URL`
- 迁移方式：
  - 生产部署显式设置 `CANONICAL_APP_URL=https://your-public-origin`
  - 保留 `NEXT_PUBLIC_APP_URL` 作为兼容回退，但推荐后端统一读取 `CANONICAL_APP_URL`

### 2026-04-17 任务 6：AI 成功结果优先落自有存储

- 变更范围：
  - `src/lib/aiTaskFlow.ts`
  - `src/collections/Media.ts`
  - `tests/aiTaskFlowStorage.test.ts`
- 调整内容：
  - AI 成功建模时，优先下载 provider 返回的模型文件并创建本地 `media` 记录。
  - `Models.formats.file` 现在会优先关联新建的 `media`，从而让现有下载链优先走本地资源。
  - 当远程文件拉取或本地 `media` 入库失败时，不中断成功主流程，而是显式记录 warning，并保留 `file: null` 的 fallback 结果。
- 默认值与来源：
  - 模型格式仍以 provider 返回的 `modelUrls` 为输入来源。
  - 本地媒体文件 `alt` 默认按 `TASK_CODE + FORMAT` 生成。
- 迁移方式：
  - 无数据库迁移。
  - 旧模型仍兼容远程 URL 下载；新成功任务会优先尝试落本地 `media`。

### 2026-04-17 任务 7：细化后台角色权限

- 变更范围：
  - `src/collections/Credits.ts`
  - `src/collections/CreditTransactions.ts`
  - `src/collections/BillingSubscriptions.ts`
  - `src/collections/ShopifyPayments.ts`
  - `src/collections/Users.ts`
  - `src/globals/SiteSettings.ts`
  - `src/globals/EmailSettings.ts`
  - `tests/roleAccess.test.ts`
- 调整内容：
  - 高敏账务写入权限从 `admin/operator` 收紧为 `admin`：
    - `credits`
    - `credit-transactions`
    - `billing-subscriptions`
    - `shopify-payments`
  - `users.creditsBalance` 字段改为仅 `admin` 可写，避免 `operator` 直接调整高敏账务。
  - 系统级站点配置和邮件配置改为 `admin` 才能更新。
  - `operator` 继续保留：
    - 后台进入权限
    - `print-orders` 运营更新
    - `homepage-content` 内容更新
- 角色矩阵：
  - `admin`：系统配置、高敏账务、角色与敏感字段写入
  - `operator`：后台运营、内容管理、订单处理、只读高危配置
  - `customer`：仅自身资源与公开入口

### 2026-04-17 任务 11：后端单元测试扩展

- 现有覆盖范围补充确认：
  - 已覆盖 `requestSecurity`、`remoteAssetSecurity`、`getCanonicalAppURL`
  - 已覆盖存储配置、签名下载、权限边界、账本基础流程、限流、Webhook 签名
- 本轮新增覆盖范围：
  - `webhookSignature` 失败分支：
    - `MISSING_SECRET`
    - `MISSING_SIGNATURE`
    - `MALFORMED_TIMESTAMP`
  - payment helper 纯函数边界：
    - provider label 映射
    - legacy patch 值裁剪
  - 账务层可隔离逻辑：
    - `refundDownloadCredits` 仅在存在对应扣费流水时生效
- 变更文件：
  - `tests/webhookSignature.test.ts`
  - `tests/paymentRecords.test.ts`
  - `tests/creditLedger.test.ts`

### 2026-04-17 任务 12：后端集成测试第一批

- 测试方案：
  - 继续沿用现有 `node:test`
  - 不引入重型测试框架
  - 通过轻量测试注入点 mock 外部依赖，直接调用后端 endpoint / flow 主链路
- 新增覆盖场景：
  - Stripe webhook 完成打印订单支付
  - 订阅发积分幂等
  - 模型下载失败自动退款
  - AI webhook 验签 + 任务更新
- 变更文件：
  - `tests/backendIntegration.test.ts`
  - `src/endpoints/stripeWebhook.ts`
  - `src/endpoints/aiTasks.ts`
  - `src/endpoints/mockDownloads.ts`
  - `src/lib/subscriptionFlow.ts`
- 运行方式：
  - 直接通过 `pnpm test:unit` 进入现有测试集合运行

### 2026-04-18 后台运营增强任务 1：运营首页 Dashboard

- 变更范围：
  - `src/lib/adminDashboard.ts`
  - `src/components/admin/OpsDashboard.tsx`
  - `src/endpoints/opsDashboard.ts`
  - `src/payload.config.ts`
- 调整内容：
  - 补齐运营首页核心卡片：
    - 今日新注册用户
    - 今日生成任务数
    - 今日成功任务数
    - 今日失败任务数
    - 今日支付成功数
    - 今日新增订单数
    - 待处理打印订单数
    - 积分异常用户数
  - 补齐 7 日趋势：
    - 任务趋势
    - 支付趋势
    - 订单趋势
  - 保留并强化状态分布：
    - 任务状态分布
    - 订单状态分布
  - 增加运营快捷按钮：
    - 查看失败任务
    - 查看待支付订单
    - 查看异常支付
    - 查看低余额用户
    - 导出运营日报
  - 新增 `opsDailyReportExportEndpoint`，使用现有 Payload endpoint 导出 CSV 日报，不破坏现有 API 路径。
- 默认值与来源：
  - Dashboard 所有指标来自现有 collections 的聚合查询：
    - `users`
    - `generation-tasks`
    - `print-orders`
    - `shopify-payments`
    - `credits`
  - “积分异常用户数” 当前按：
    - `reservedBalance > 0`
    - 或 `balance <= 20`
    计算。
- 迁移方式：
  - 无数据库迁移
  - 无新增配置
  - Dashboard 打开即可生效，导出按钮走 `/api/platform/ops/dashboard/export`

### 2026-04-18 后台运营增强任务 2：用户管理页增强

- 变更范围：
  - `src/collections/Users.ts`
  - `src/lib/adminUsers.ts`
  - `src/endpoints/adminUsers.ts`
  - `src/payload.config.ts`
  - `tests/adminUsers.test.ts`
- 调整内容：
  - 为用户增加运营状态字段：
    - `accountStatus`
    - `disabledAt`
    - `disabledReason`
    - `disabledBy`
  - 增强用户列表默认列，方便后台运营直接查看账户状态与最近活跃。
  - 新增用户运营工作台后端聚合能力，返回：
    - 基础资料
    - 积分账户
    - 积分流水
    - 订阅记录
    - 任务记录
    - 模型列表
    - 订单记录
    - 支付记录
    - 审计日志占位区
  - 新增运营动作 endpoint：
    - 查看工作台详情
    - 禁用账号
    - 恢复账号
    - 生成重置密码链接
    - 切换角色（admin only）
- 默认值与来源：
  - `accountStatus`：默认 `active`
  - `disabledAt` / `disabledReason` / `disabledBy`：默认空
  - 重置密码链接来源：
    - `CANONICAL_APP_URL`
    - 回退 `NEXT_PUBLIC_APP_URL`
    - 开发环境再回退本地地址（沿用现有 canonical helper）
- 迁移方式：
  - 需要更新用户表结构（新增字段）
  - 不删除任何现有兼容字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/users/:userId/workspace`
    - `/api/platform/admin/users/:userId/disable`
    - `/api/platform/admin/users/:userId/restore`
    - `/api/platform/admin/users/:userId/reset-password-link`
    - `/api/platform/admin/users/:userId/role`

### 2026-04-18 后台运营增强任务 3：任务管理页增强

- 变更范围：
  - `src/collections/GenerationTasks.ts`
  - `src/lib/aiTaskFlow.ts`
  - `src/lib/adminTasks.ts`
  - `src/endpoints/adminTasks.ts`
  - `src/payload.config.ts`
  - `tests/adminTasks.test.ts`
- 调整内容：
  - 增强任务列表默认列，直接显示：
    - provider
    - 预扣积分
    - 实扣积分
  - 新增任务工作台后端聚合能力，返回：
    - 原始任务信息
    - source image
    - prompt
    - providerTaskId
    - callback payload
    - task events
    - 账务记录
    - 结果模型
    - 错误详情
  - 新增运营动作 endpoint：
    - 重新同步任务状态
    - 人工标记失败
    - 人工重试任务
    - 重新触发结果入库
    - 导出任务详情
  - 所有动作均写入：
    - task events
    - audit log
- 默认值与来源：
  - 无新增配置
  - “人工重试任务”输入来源于原任务：
    - `inputMode`
    - `prompt`
    - `provider`
    - `sourceImage`
    - `printRequested`
    - `parameterSnapshot`（会自动移除运行态 `billing` / `meshy` 快照）
- 迁移方式：
  - 无数据库迁移
  - 不删除任何现有兼容字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/tasks/:taskId/workspace`
    - `/api/platform/admin/tasks/:taskId/resync`
    - `/api/platform/admin/tasks/:taskId/mark-failed`
    - `/api/platform/admin/tasks/:taskId/retry`
    - `/api/platform/admin/tasks/:taskId/reingest`
    - `/api/platform/admin/tasks/:taskId/export`

### 2026-04-18 后台运营增强任务 4：支付记录页增强

- 变更范围：
  - `src/collections/ShopifyPayments.ts`
  - `src/lib/adminPayments.ts`
  - `src/endpoints/adminPayments.ts`
  - `src/payload.config.ts`
  - `tests/adminPayments.test.ts`
- 调整内容：
  - 为支付记录增加运营异常处理字段：
    - `exceptionFlag`
    - `exceptionReason`
    - `handledAt`
    - `handledBy`
  - 增强支付列表默认列，直接显示 session 与异常标记。
  - 新增支付工作台后端聚合能力，返回：
    - 原始 payment
    - 中性字段：
      - `provider`
      - `sessionId`
      - `checkoutUrl`
      - `orderReference`
      - `paymentType`
      - `status`
    - 原始 webhook payload
    - 关联订单
    - 用户
    - 审计日志占位区
  - 新增运营动作 endpoint：
    - 重新同步支付状态
    - 标记异常
    - 导出支付详情
  - 所有动作写审计日志。
- 默认值与来源：
  - `exceptionFlag`：默认 `false`
  - `exceptionReason` / `handledAt` / `handledBy`：默认空
  - `provider` / `sessionId` / `checkoutUrl` / `orderReference` 全部来自现有支付兼容层 helper，不新增破坏性字段
- 迁移方式：
  - 需要更新支付记录表结构（新增异常处理字段）
  - 不删除任何历史兼容字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/payments/:paymentId/workspace`
    - `/api/platform/admin/payments/:paymentId/resync`
    - `/api/platform/admin/payments/:paymentId/mark-exception`
    - `/api/platform/admin/payments/:paymentId/export`

### 2026-04-18 后台运营增强任务 5：打印订单页增强

- 变更范围：
  - `src/collections/PrintOrders.ts`
  - `src/lib/adminOrders.ts`
  - `src/endpoints/adminOrders.ts`
  - `src/payload.config.ts`
  - `tests/adminOrders.test.ts`
- 调整内容：
  - 为订单增加履约状态操作元字段：
    - `statusUpdatedAt`
    - `statusUpdatedBy`
  - 增强订单列表默认列，直接显示模型、材质、尺寸。
  - 新增订单工作台后端聚合能力，返回：
    - 基础订单
    - 收货信息
    - 关联模型
    - 关联任务
    - 关联支付记录
    - 审计日志占位区
  - 新增运营动作 endpoint：
    - 标记生产中
    - 标记已发货
    - 标记已完成
    - 标记已取消
    - 导出订单详情
  - 所有状态流转写入审计日志。
- 默认值与来源：
  - `statusUpdatedAt` / `statusUpdatedBy` 默认空
  - 订单履约状态流转来源于后台运营动作 endpoint
- 迁移方式：
  - 需要更新订单表结构（新增状态操作元字段）
  - 不删除任何历史兼容字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/orders/:orderId/workspace`
    - `/api/platform/admin/orders/:orderId/in-production`
    - `/api/platform/admin/orders/:orderId/shipped`
    - `/api/platform/admin/orders/:orderId/completed`
    - `/api/platform/admin/orders/:orderId/cancelled`
    - `/api/platform/admin/orders/:orderId/export`

### 2026-04-18 后台运营增强任务 6：积分管理页增强

- 变更范围：
  - `src/collections/Credits.ts`
  - `src/collections/CreditTransactions.ts`
  - `src/lib/creditLedger.ts`
  - `src/lib/adminCredits.ts`
  - `src/endpoints/adminCredits.ts`
  - `src/payload.config.ts`
  - `tests/adminCredits.test.ts`
- 调整内容：
  - 为积分账户 / 流水增加异常处理字段：
    - `exceptionFlag`
    - `exceptionReason`
    - `reviewedAt`
    - `reviewedBy`
  - 增强后台列表默认列，直接显示异常标记、余额与幂等键等关键字段。
  - 在账本层新增 `manualAdjustCredits()`，支持 admin 手工调账。
  - 新增积分工作台后端聚合能力，返回：
    - 账户
    - 最近流水
    - 关联用户
    - 审计日志占位区
  - 新增后台动作 endpoint：
    - 手工调账（admin only）
    - 标记异常账务
    - 导出流水 CSV
  - 所有手工调账与异常标记写审计日志。
- 默认值与来源：
  - `exceptionFlag` 默认 `false`
  - `exceptionReason / reviewedAt / reviewedBy` 默认空
  - 手工调账来源：
    - 后台 admin endpoint
- 迁移方式：
  - 需要更新 `credits` 和 `credit-transactions` 表结构（新增异常处理字段）
  - 不删除任何历史账务字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/credits/:creditId/workspace`
    - `/api/platform/admin/credits/:creditId/adjust`
    - `/api/platform/admin/credit-transactions/:transactionId/mark-exception`
    - `/api/platform/admin/credits/:creditId/export`

### 2026-04-18 后台运营增强任务 7：订阅管理页增强

- 变更范围：
  - `src/collections/BillingSubscriptions.ts`
  - `src/lib/adminSubscriptions.ts`
  - `src/endpoints/adminSubscriptions.ts`
  - `src/payload.config.ts`
  - `tests/adminSubscriptions.test.ts`
- 调整内容：
  - 为订阅记录增加同步异常处理字段：
    - `syncExceptionFlag`
    - `syncExceptionReason`
    - `lastSyncedAt`
  - 增强后台列表默认列，直接显示：
    - `cancelAtPeriodEnd`
    - `lastGrantedPeriodKey`
    - `syncExceptionFlag`
  - 新增订阅工作台后端聚合能力，返回：
    - 订阅
    - 用户
    - 关联支付记录
    - 本周期是否已发点
    - 审计日志占位区
  - 新增后台动作 endpoint：
    - 重新同步订阅
    - 检查本周期是否已发点
    - 导出订阅详情
  - 所有订阅运营动作写审计日志。
- 默认值与来源：
  - `syncExceptionFlag` 默认 `false`
  - `syncExceptionReason / lastSyncedAt` 默认空
  - 本周期发点检查来源：
    - `lastGrantedPeriodKey`
    - `stripeSubscriptionId`
    - `currentPeriodEnd`
- 迁移方式：
  - 需要更新 `billing-subscriptions` 表结构（新增同步异常字段）
  - 不删除任何历史订阅字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/subscriptions/:subscriptionId/workspace`
    - `/api/platform/admin/subscriptions/:subscriptionId/resync`
    - `/api/platform/admin/subscriptions/:subscriptionId/check-grant`
    - `/api/platform/admin/subscriptions/:subscriptionId/export`

### 2026-04-18 后台运营增强任务 8：内容运营页增强

- 变更范围：
  - `src/lib/adminContent.ts`
  - `src/endpoints/adminContent.ts`
  - `src/payload.config.ts`
  - `tests/adminContent.test.ts`
- 调整内容：
  - 新增内容运营导出能力，支持：
    - 首页内容项
    - 文章/活动帖
    - 公告
    - 专题包
  - 新增专题包批量运营动作：
    - 批量加入模型
    - 批量移除模型
  - 专题包模型批量操作写审计日志。
- 默认值与来源：
  - 无新增配置
  - 导出字段统一由现有内容集合字段映射得到：
    - `title`
    - `type`
    - `status`
    - `isVisible`
    - `sortOrder`
    - `createdBy`
    - `updatedAt`
- 迁移方式：
  - 无数据库迁移
  - 不删除任何现有内容集合
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/content/:collection/export`
    - `/api/platform/admin/model-bundles/:bundleId/add-models`
    - `/api/platform/admin/model-bundles/:bundleId/remove-models`

### 2026-04-18 后台运营增强任务 9：审计日志中心页面化

- 变更范围：
  - `src/collections/AuditLogs.ts`
  - `src/lib/auditLog.ts`
  - `src/lib/adminAudit.ts`
  - `src/endpoints/adminAudit.ts`
  - `src/payload.config.ts`
  - `tests/auditLog.test.ts`
  - `tests/adminAudit.test.ts`
- 调整内容：
  - 新增 `audit-logs` collection，作为后台审计日志中心的数据源。
  - `writeAuditLog()` 继续写 logger，同时 best-effort 写入 `audit-logs` collection。
  - 新增审计日志导出能力：
    - 列表 CSV 导出
    - 单条 JSON 导出
- 默认值与来源：
  - 无新增配置
  - 审计数据来源：
    - 现有 `writeAuditLog()` 调用点
  - `timestamp` 在写入时自动生成
- 迁移方式：
  - 需要新增 `audit-logs` 表
  - 不删除现有 logger 审计接口
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/audit/export`
    - `/api/platform/admin/audit/:logId/export`

### 2026-04-18 后台运营增强任务 10：配置中心优化

- 变更范围：
  - `src/globals/AIProviderSettings.ts`
  - `src/globals/StorageSettings.ts`
  - `src/globals/SecuritySettings.ts`
  - `src/globals/SiteSettings.ts`
  - `src/globals/RuntimeDeploymentSettings.ts`
  - `src/lib/adminConfig.ts`
  - `src/endpoints/adminConfig.ts`
  - `src/payload.config.ts`
  - `tests/adminConfig.test.ts`
- 调整内容：
  - 为高危 Global 配置补统一治理字段：
    - `effectiveMode`
    - `changeSummary`
    - `lastModifiedBy`
    - `lastValidatedAt`
  - 新增配置中心摘要能力，返回：
    - 当前生效模式
    - 最近修改人
    - 最近验证时间
  - 新增配置验证动作：
    - 仅 admin 可执行
    - 记录验证时间与修改人
    - 写审计日志
  - 新增配置详情导出
- 默认值与来源：
  - `effectiveMode` 默认 `runtime-global`
  - `changeSummary / lastModifiedBy / lastValidatedAt` 默认空
  - 无新增环境变量
- 迁移方式：
  - 需要更新各 Global 的数据结构（新增治理字段）
  - 不删除任何原有配置字段
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/config/overview`
    - `/api/platform/admin/config/:slug/validate`
    - `/api/platform/admin/config/:slug/export`

### 2026-04-18 后台运营增强任务 11：后台搜索与导出能力

- 变更范围：
  - `src/lib/adminSearch.ts`
  - `src/endpoints/adminSearch.ts`
  - `src/payload.config.ts`
  - `tests/adminSearch.test.ts`
- 调整内容：
  - 新增统一后台搜索能力，支持域：
    - `users`
    - `tasks`
    - `payments`
    - `orders`
    - `subscriptions`
    - `audit`
  - 支持：
    - 关键词搜索
    - 精确搜索
    - 状态筛选
    - 用户维度筛选
    - 时间区间筛选
  - 新增统一列表导出能力，支持：
    - 用户列表
    - 积分流水
    - 任务列表
    - 支付记录
    - 打印订单
    - 订阅记录
    - 审计日志
- 默认值与来源：
  - 无新增配置
  - 搜索与导出数据全部来自现有 collections
- 迁移方式：
  - 无数据库迁移
  - 不删除任何已有详情导出能力
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/search`
    - `/api/platform/admin/export/:domain`

### 2026-04-18 后台运营增强任务 12：异常处理工作台

- 变更范围：
  - `src/lib/adminExceptions.ts`
  - `src/endpoints/adminExceptions.ts`
  - `src/payload.config.ts`
  - `tests/adminExceptions.test.ts`
- 调整内容：
  - 新增异常工作台总览，集中输出四类异常：
    - 异常任务
    - 异常支付
    - 异常账务
    - 异常订单
  - 新增单类异常导出能力：
    - `tasks`
    - `payments`
    - `orders`
    - `accounting`
- 默认值与来源：
  - 无新增配置
  - 数据全部来自现有 collections 聚合查询
- 迁移方式：
  - 无数据库迁移
  - 不删除任何已有异常筛查能力
  - 新增 endpoint 直接生效：
    - `/api/platform/admin/exceptions`
    - `/api/platform/admin/exceptions/:domain/export`
