/**
 * Next.js client instrumentation (Next 15.3+). @sentry/nextjs v9+ loads
 * browser-side Sentry through this file rather than auto-injecting
 * sentry.client.config.ts.
 */
import '../sentry.client.config'
